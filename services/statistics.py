class Statistics:

    @staticmethod
    def average(values):

        return sum(values) / len(values)

    @staticmethod
    def maximum(values):

        return max(values)

    @staticmethod
    def minimum(values):

        return min(values)
    